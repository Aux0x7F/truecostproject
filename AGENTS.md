# Agent Instructions

Always abide by `./CONTRACT.md`. If it cannot be located, ask for it before proceeding unless the user grants a per-chat exception.

For all repo work, follow the local repo skill:

```text
.skills/truecost-site-operator/SKILL.md
```

Do not use `constitute-workspace`; that belongs to a different project family.

For fresh no-code users, first check GitHub connection, push access, and Node availability. If those pass, launch the editor in the side browser before giving the short "what can I help with" intro.
